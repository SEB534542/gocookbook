<!DOCTYPE html>
<html>
	<head>
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Recept voor {{.Name}}</title>
		{{template "style"}}
	</head>	
	<body>
		<p>
			<a href="/">Alle recepten</a> | <a href="/edit/{{.Id}}">Pas recept aan</a> | <a href="/conv">Werk conversie tabel bij</a>
		</p>
		<p style="font-size:10vw">
			<h1>{{.Name}}</h1>
			<p><i>{{.Notes}}</i><p>
			<form method="POST">
				<label for="Persons">Aantal personen</label>
				<input type="number" name="Persons" value="{{.Persons}}" required>
				<input type="submit" value="Pas aan"><br>
			</form>
			<h2>Ingrediënten</h2>
				<table>
					{{range .Ingrs}}
						<tr>
							<td><input type="checkbox"></td>
							<td>{{.Amount}} {{.Unit}} {{.Item}} {{.Notes}} {{if .AltUnits}}({{.AltUnits}}){{end}}</td>
						</tr>
					{{end}}
				</table>				
			<h2>Stappen</h2>
			<ol>
				{{range .Steps}}
					<li>{{.}}</li>
				{{end}}
			</ol>
			<br>
			{{if ne .Source ""}}
				<i>Recept bron: {{.Source}}</i>
			{{end}}
		</p>
	</body>
</html>